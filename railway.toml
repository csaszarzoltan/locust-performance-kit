[build]
builder = "railpack"

[deploy]
startCommand = "sh -c 'exec locust -f src/locust_templates/api_load.py --web-host 0.0.0.0 --web-port ${PORT:-8089}'"
healthcheckPath = "/"
healthcheckTimeout = 300
